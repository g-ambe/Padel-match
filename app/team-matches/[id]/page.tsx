"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { useParams, useRouter } from "next/navigation";
import { ActionButton, Card } from "@/components/ui";
import { fetchActiveClubMemberParticipants } from "@/lib/event-participants";
import { createShareToken, getOfficialAccess, officialStatusLabel, type OfficialGroup } from "@/lib/official-matches";
import { getSupabaseClient } from "@/lib/supabase";

type ResultValue = "win" | "lose" | "draw" | "undecided";
type MemberOption = { playerProfileId: string; displayName: string | null };
type EventRow = { id: string; name: string; status: string; club_id: string | null; description: string | null; memo: string | null; stats_mode: string; share_enabled?: boolean | null; share_token?: string | null; created_by_auth_user_id?: string | null; is_deleted?: boolean | null };
type Side = { id: string; side: "team_a" | "team_b"; club_id: string | null; team_name: string | null };
type Match = { id: string; match_order: number; team_a_player1_profile_id: string | null; team_a_player1_guest_name: string | null; team_a_player2_profile_id: string | null; team_a_player2_guest_name: string | null; team_b_player1_profile_id: string | null; team_b_player1_guest_name: string | null; team_b_player2_profile_id: string | null; team_b_player2_guest_name: string | null; team_a_score: number | null; team_b_score: number | null; result: ResultValue; score_detail: string | null; memo: string | null; youtube_url: string | null; created_at?: string | null };
type Form = { a1ProfileId: string; a1GuestName: string; a2ProfileId: string; a2GuestName: string; b1ProfileId: string; b1GuestName: string; b2ProfileId: string; b2GuestName: string; aScore: string; bScore: string; result: ResultValue; scoreDetail: string; memo: string; youtubeUrl: string };
const emptyForm = (): Form => ({ a1ProfileId:"",a1GuestName:"",a2ProfileId:"",a2GuestName:"",b1ProfileId:"",b1GuestName:"",b2ProfileId:"",b2GuestName:"",aScore:"",bScore:"",result:"undecided",scoreDetail:"",memo:"",youtubeUrl:"" });
const resultLabel = (r: ResultValue) => ({ win:"自チーム勝ち", lose:"相手チーム勝ち", draw:"引き分け", undecided:"未定" }[r]);
const autoResult = (a: string, b: string): ResultValue => { if (a.trim()==="" || b.trim()==="") return "undecided"; const x=Number(a), y=Number(b); if (!Number.isInteger(x)||!Number.isInteger(y)) return "undecided"; if (x>y) return "win"; if (x<y) return "lose"; return "draw"; };
const isYoutubeUrl = (v: string) => /^https:\/\/(www\.)?youtube\.com\/watch\?v=/.test(v) || /^https:\/\/youtu\.be\//.test(v) || /^https:\/\/(www\.)?youtube\.com\/shorts\//.test(v);

export default function FriendlyTeamMatchDetailPage() {
  const { id } = useParams<{ id: string }>(); const router = useRouter();
  const [event, setEvent] = useState<EventRow | null>(null); const [groups,setGroups]=useState<OfficialGroup[]>([]); const [sides,setSides]=useState<Side[]>([]); const [members,setMembers]=useState<Record<string, MemberOption[]>>({ team_a: [], team_b: [] }); const [matches,setMatches]=useState<Match[]>([]);
  const [form,setForm]=useState<Form>(emptyForm()); const [editingId,setEditingId]=useState<string|null>(null); const [editingForm,setEditingForm]=useState<Form>(emptyForm()); const [error,setError]=useState(""); const [notice,setNotice]=useState(""); const [isSuperUser,setIsSuperUser]=useState(false); const [isCreator,setIsCreator]=useState(false); const [canManage,setCanManage]=useState(false); const [showForm,setShowForm]=useState(false); const [showDelete,setShowDelete]=useState(false); const [deleteOk,setDeleteOk]=useState(false); const [closeMode,setCloseMode]=useState<"official"|"record_only">("official");
  const teamA = sides.find((s)=>s.side==="team_a"); const teamB = sides.find((s)=>s.side==="team_b");
  const shareUrl = useMemo(()=> event?.status === "closed" && event.share_enabled && event.share_token && typeof window !== "undefined" ? `${window.location.origin}/share/events/${event.share_token}` : "", [event]);
  const load = async () => { const supabase=getSupabaseClient(); if(!supabase) return; const access=await getOfficialAccess(supabase); setIsSuperUser(access.superUser); setGroups(access.groups.filter((g)=>access.superUser||g.role!=="member")); const { data:e }=await supabase.from("events").select("id,name,status,club_id,description,memo,stats_mode,share_enabled,share_token,created_by_auth_user_id,is_deleted,event_mode").eq("id",id).eq("event_mode","team").maybeSingle(); const row=e as any as EventRow|null; if(!row || row.is_deleted){setError("フレンドリーチームマッチが見つかりません"); return;} const creator=access.uid===row.created_by_auth_user_id; const manageable=access.superUser||creator||access.groups.some((g)=>g.id===row.club_id&&(g.role==="main_admin"||g.role==="sub_admin")); setEvent(row); setIsCreator(creator); setCanManage(manageable); const { data:ss }=await supabase.from("event_team_sides").select("id,side,club_id,team_name").eq("event_id",id); const sideRows=(ss??[]) as Side[]; setSides(sideRows); const loaded: Record<string, MemberOption[]>={team_a:[],team_b:[]}; for (const side of sideRows) if (side.club_id) loaded[side.side]=await fetchActiveClubMemberParticipants(supabase, side.club_id); setMembers(loaded); const { data:ms }=await supabase.from("event_team_matches").select("*").eq("event_id",id).order("match_order").order("created_at").order("id"); setMatches((ms??[]) as Match[]); };
  useEffect(()=>{void load();},[id]);
  const memberName=(side:"team_a"|"team_b", pid:string|null, guest:string|null)=>guest||members[side].find((m)=>m.playerProfileId===pid)?.displayName||"未入力";
  const validate=(f:Form)=>{ const vals=[f.a1ProfileId||f.a1GuestName.trim(),f.a2ProfileId||f.a2GuestName.trim(),f.b1ProfileId||f.b1GuestName.trim(),f.b2ProfileId||f.b2GuestName.trim()].filter(Boolean); if(new Set(vals).size!==vals.length) return "同一試合内で同じ選手は選択できません"; if([f.a1ProfileId&&f.a1GuestName.trim(),f.a2ProfileId&&f.a2GuestName.trim(),f.b1ProfileId&&f.b1GuestName.trim(),f.b2ProfileId&&f.b2GuestName.trim()].some(Boolean)) return "選手は選択またはゲスト名のどちらかにしてください"; if((f.aScore&&(!Number.isInteger(Number(f.aScore))||Number(f.aScore)<0))||(f.bScore&&(!Number.isInteger(Number(f.bScore))||Number(f.bScore)<0))) return "得点は0以上の整数で入力してください"; if(f.youtubeUrl.trim()&&!isYoutubeUrl(f.youtubeUrl.trim())) return "YouTubeのURLを入力してください"; return ""; };
  const payload=(f:Form)=>({ team_a_player1_profile_id:f.a1ProfileId||null, team_a_player1_guest_name:f.a1GuestName.trim()||null, team_a_player2_profile_id:f.a2ProfileId||null, team_a_player2_guest_name:f.a2GuestName.trim()||null, team_b_player1_profile_id:f.b1ProfileId||null, team_b_player1_guest_name:f.b1GuestName.trim()||null, team_b_player2_profile_id:f.b2ProfileId||null, team_b_player2_guest_name:f.b2GuestName.trim()||null, team_a_score:f.aScore.trim()===""?null:Number(f.aScore), team_b_score:f.bScore.trim()===""?null:Number(f.bScore), result:f.result, score_detail:f.scoreDetail.trim()||null, memo:f.memo.trim()||null, youtube_url:f.youtubeUrl.trim()||null, updated_at:new Date().toISOString() });
  const toForm=(m:Match):Form=>({ a1ProfileId:m.team_a_player1_profile_id??"",a1GuestName:m.team_a_player1_guest_name??"",a2ProfileId:m.team_a_player2_profile_id??"",a2GuestName:m.team_a_player2_guest_name??"",b1ProfileId:m.team_b_player1_profile_id??"",b1GuestName:m.team_b_player1_guest_name??"",b2ProfileId:m.team_b_player2_profile_id??"",b2GuestName:m.team_b_player2_guest_name??"",aScore:m.team_a_score===null?"":String(m.team_a_score),bScore:m.team_b_score===null?"":String(m.team_b_score),result:m.result,scoreDetail:m.score_detail??"",memo:m.memo??"",youtubeUrl:m.youtube_url??""});
  const saveMatch=async(edit=false)=>{setError("");setNotice(""); if(!canManage||event?.status==="closed") return setError("この操作を行う権限がありません"); const f=edit?editingForm:form; const v=validate(f); if(v)return setError(v); const supabase=getSupabaseClient(); if(!supabase)return; const err= edit&&editingId ? (await supabase.from("event_team_matches").update(payload(f)).eq("id",editingId)).error : (await supabase.from("event_team_matches").insert({event_id:id,match_order:Math.max(0,...matches.map((m)=>m.match_order))+1,...payload(f)})).error; if(err)return setError(edit?"試合カードの更新に失敗しました":"試合カードの追加に失敗しました"); setForm(emptyForm()); setEditingId(null); setEditingForm(emptyForm()); setShowForm(false); setNotice(edit?"試合カードを更新しました":"試合カードを追加しました"); await load(); };
  const deleteMatch=async(mid:string)=>{ if(!window.confirm("試合カードを削除しますか？"))return; const supabase=getSupabaseClient(); if(!supabase)return; const {error:e}=await supabase.from("event_team_matches").delete().eq("id",mid); if(e)return setError("試合カードの削除に失敗しました"); setNotice("試合カードを削除しました"); await load(); };
  const updateSide=async(side:Side, clubId:string, name:string)=>{ const supabase=getSupabaseClient(); if(!supabase)return; await supabase.from("event_team_sides").update({club_id:clubId||null,team_name:name.trim()||null,updated_at:new Date().toISOString()}).eq("id",side.id); await load(); };
  const closeOrReopen=async(status:"active"|"closed")=>{ const supabase=getSupabaseClient(); if(!supabase)return; if(status==="closed"&&!window.confirm("フレンドリーチームマッチ終了しますか？"))return; const {error:e}=await supabase.from("events").update({status,stats_mode:status==="closed"?closeMode:"undecided",closed_at:status==="closed"?new Date().toISOString():null,updated_at:new Date().toISOString()}).eq("id",id); if(e)return setError(status==="closed"?"終了に失敗しました":"再開に失敗しました"); await load(); };
  const deleteEvent=async()=>{ if(!deleteOk)return setError("削除確認にチェックしてください"); const supabase=getSupabaseClient(); if(!supabase)return; await supabase.from("events").update({is_deleted:true,deleted_at:new Date().toISOString(),share_enabled:false,share_token:null}).eq("id",id); router.push("/team-matches/new"); };
  const share=async()=>{ const supabase=getSupabaseClient(); if(!supabase)return; await supabase.from("events").update({share_enabled:true,share_token:createShareToken(),share_token_updated_at:new Date().toISOString()}).eq("id",id); await load(); };
  if(!event) return <main className="mx-auto min-h-screen w-full max-w-md p-4 text-zinc-100">{error||"読み込み中..."}</main>;
  return <main className="mx-auto flex min-h-screen w-full max-w-md flex-col gap-4 p-4 text-zinc-100"><h1 className="text-xl font-bold">フレンドリーチームマッチ詳細</h1>{error&&<p className="rounded-xl border border-red-500/60 bg-red-950/40 p-3 text-sm text-red-200">{error}</p>}{notice&&<p className="rounded-xl border border-emerald-500/60 bg-emerald-950/40 p-3 text-sm text-emerald-200">{notice}</p>}<Card title={event.name}><dl className="space-y-2 text-sm"><div><dt className="text-zinc-400">ステータス</dt><dd>{officialStatusLabel(event.status)}</dd></div><div><dt className="text-zinc-400">説明</dt><dd className="whitespace-pre-wrap">{event.description||"未入力"}</dd></div><div><dt className="text-zinc-400">メモ</dt><dd className="whitespace-pre-wrap">{event.memo||"未入力"}</dd></div></dl></Card><Card title="チーム設定"><div className="space-y-3">{[teamA,teamB].filter(Boolean).map((s)=><SideEditor key={s!.id} side={s!} title={s!.side==="team_a"?"自チーム":"相手チーム"} groups={groups} canManage={canManage&&event.status!=="closed"} onSave={updateSide}/>)}</div></Card><Card title="開催操作"><div className="space-y-3 text-sm">{event.status==="active"&&canManage&&<><select className="w-full rounded-xl bg-zinc-800 p-3" value={closeMode} onChange={(e)=>setCloseMode(e.target.value as any)}><option value="official">戦績に反映する</option><option value="record_only">記録用にする</option></select><button className="w-full rounded-xl border border-red-500/70 py-2 font-bold text-red-200" onClick={()=>void closeOrReopen("closed")}>フレンドリーチームマッチ終了</button></>}{event.status==="closed"&&<><p>フレンドリーチームマッチ終了済み（{event.stats_mode==="record_only"?"記録用":"戦績に反映する"}）</p>{canManage&&<button className="w-full rounded-xl bg-accent py-2 font-bold text-black" onClick={()=>void closeOrReopen("active")}>フレンドリーチームマッチ再開</button>}{canManage&&(showDelete?<div className="space-y-2 rounded-xl border border-red-500/60 p-3"><label className="flex gap-2 text-xs"><input type="checkbox" checked={deleteOk} onChange={(e)=>setDeleteOk(e.target.checked)}/>削除すると元に戻せません</label><button className="w-full rounded bg-red-600 py-2 font-bold" onClick={()=>void deleteEvent()}>フレンドリーチームマッチ削除</button></div>:<button className="w-full rounded-xl border border-red-500/70 py-2 font-bold text-red-200" onClick={()=>setShowDelete(true)}>フレンドリーチームマッチ削除</button>)}</>}</div></Card>{event.status==="closed"&&<Card title="共有リンク"><div className="space-y-2 text-sm">{shareUrl?<><p className="break-all rounded-xl bg-zinc-800 p-3 text-xs">{shareUrl}</p><button className="w-full rounded-xl border border-zinc-500 py-2" onClick={()=>navigator.clipboard.writeText(shareUrl)}>共有リンクをコピー</button></>:canManage?<button className="w-full rounded-xl bg-accent py-2 font-bold text-black" onClick={()=>void share()}>共有リンクを作成</button>:<p>共有されていません</p>}</div></Card>}<Card title="試合結果"><div className="space-y-3">{matches.map((m,i)=><div key={m.id} className="rounded-xl bg-zinc-800 p-3 text-sm"><p className="font-bold">第{i+1}試合</p><p>{memberName("team_a",m.team_a_player1_profile_id,m.team_a_player1_guest_name)} / {memberName("team_a",m.team_a_player2_profile_id,m.team_a_player2_guest_name)} vs {memberName("team_b",m.team_b_player1_profile_id,m.team_b_player1_guest_name)} / {memberName("team_b",m.team_b_player2_profile_id,m.team_b_player2_guest_name)}</p><p>スコア: {m.team_a_score??"未入力"} - {m.team_b_score??"未入力"}</p><p>結果: {resultLabel(m.result)}</p>{m.score_detail&&<p>詳細スコア: {m.score_detail}</p>}{m.memo&&<p className="whitespace-pre-wrap">メモ: {m.memo}</p>}{m.youtube_url&&<a className="underline" href={m.youtube_url} target="_blank" rel="noreferrer">動画視聴</a>}{canManage&&event.status!=="closed"&&<div className="mt-2 grid grid-cols-2 gap-2"><button className="rounded border border-zinc-500 py-2" onClick={()=>{setEditingId(m.id);setEditingForm(toForm(m));}}>編集</button><button className="rounded border border-red-500/70 py-2 text-red-200" onClick={()=>void deleteMatch(m.id)}>削除</button></div>}{editingId===m.id&&<MatchFormView title="試合カードを編集" form={editingForm} setForm={(p,s)=>setEditingForm((prev)=>{const n={...prev,...p}; if(s)n.result=autoResult(n.aScore,n.bScore); return n;})} teamAMembers={members.team_a} teamBMembers={members.team_b} onSave={()=>void saveMatch(true)} onCancel={()=>setEditingId(null)}/>}</div>)}{canManage&&event.status!=="closed"&&(showForm?<MatchFormView title="試合カードを追加" form={form} setForm={(p,s)=>setForm((prev)=>{const n={...prev,...p}; if(s)n.result=autoResult(n.aScore,n.bScore); return n;})} teamAMembers={members.team_a} teamBMembers={members.team_b} onSave={()=>void saveMatch(false)} onCancel={()=>setShowForm(false)}/>:<ActionButton onClick={()=>setShowForm(true)}>試合カードを追加</ActionButton>)}</div></Card><Link href="/team-matches/new" className="text-center text-sm underline">フレンドリーチームマッチへ戻る</Link></main>;
}
function SideEditor({side,title,groups,canManage,onSave}:{side:Side;title:string;groups:OfficialGroup[];canManage:boolean;onSave:(s:Side,c:string,n:string)=>void}){const[mode,setMode]=useState<"group"|"free">(side.club_id?"group":"free");const[clubId,setClubId]=useState(side.club_id??"");const[name,setName]=useState(side.team_name??title);return <section className="space-y-2 rounded-xl bg-zinc-800 p-3"><p className="font-bold">{title}</p><select disabled={!canManage} className="w-full rounded-xl bg-zinc-900 p-3" value={mode} onChange={(e)=>{const next=e.target.value as "group"|"free"; setMode(next); if(next==="free") setClubId("");}}><option value="group">グループを選択する</option><option value="free">グループなしで入力する</option></select>{mode==="group"&&<select disabled={!canManage} className="w-full rounded-xl bg-zinc-900 p-3" value={clubId} onChange={(e)=>setClubId(e.target.value)}><option value="">グループを選択する</option>{groups.map((g)=><option key={g.id} value={g.id}>{g.name}</option>)}</select>}<input disabled={!canManage} className="w-full rounded-xl bg-zinc-900 p-3" placeholder="チーム名" value={name} onChange={(e)=>setName(e.target.value)}/>{canManage&&<button className="w-full rounded-xl border border-zinc-500 py-2" onClick={()=>onSave(side,mode==="group"?clubId:"",name)}>保存</button>}</section>}
function MatchFormView({title,form,setForm,teamAMembers,teamBMembers,onSave,onCancel}:{title:string;form:Form;setForm:(p:Partial<Form>,sync?:boolean)=>void;teamAMembers:MemberOption[];teamBMembers:MemberOption[];onSave:()=>void;onCancel:()=>void}){const cls="w-full rounded-xl border border-zinc-700 bg-zinc-950/70 px-3 py-2 text-sm"; const player=(label:string,pid:keyof Form,guest:keyof Form,members:MemberOption[])=><div className="space-y-1"><label className="text-xs text-zinc-300">{label}</label><select className={cls} value={form[pid] as string} onChange={(e)=>setForm({[pid]:e.target.value} as any)}><option value="">グループメンバーから選択</option>{members.map((m)=><option key={m.playerProfileId} value={m.playerProfileId}>{m.displayName??"名称未設定"}</option>)}</select><input className={cls} placeholder="ゲストを追加" value={form[guest] as string} onChange={(e)=>setForm({[guest]:e.target.value} as any)}/></div>; return <div className="space-y-3 rounded-2xl border border-zinc-700 p-3"><h4 className="font-bold">{title}</h4><section className="space-y-2"><p className="text-xs font-bold text-zinc-400">自チーム</p>{player("自チーム選手1","a1ProfileId","a1GuestName",teamAMembers)}{player("自チーム選手2","a2ProfileId","a2GuestName",teamAMembers)}</section><section className="space-y-2"><p className="text-xs font-bold text-zinc-400">相手チーム</p>{player("相手チーム選手1","b1ProfileId","b1GuestName",teamBMembers)}{player("相手チーム選手2","b2ProfileId","b2GuestName",teamBMembers)}</section><div className="grid grid-cols-2 gap-2"><input className={cls} inputMode="numeric" placeholder="自チーム得点" value={form.aScore} onChange={(e)=>setForm({aScore:e.target.value},true)}/><input className={cls} inputMode="numeric" placeholder="相手チーム得点" value={form.bScore} onChange={(e)=>setForm({bScore:e.target.value},true)}/></div><select className={cls} value={form.result} onChange={(e)=>setForm({result:e.target.value as ResultValue})}><option value="win">自チーム勝ち</option><option value="lose">相手チーム勝ち</option><option value="draw">引き分け</option><option value="undecided">未定</option></select><input className={cls} placeholder="詳細スコア" value={form.scoreDetail} onChange={(e)=>setForm({scoreDetail:e.target.value})}/><textarea className={cls} placeholder="メモ" value={form.memo} onChange={(e)=>setForm({memo:e.target.value})}/><input className={cls} placeholder="YouTubeリンク" value={form.youtubeUrl} onChange={(e)=>setForm({youtubeUrl:e.target.value})}/><div className="grid grid-cols-2 gap-2"><button className="rounded-xl bg-accent py-2 font-bold text-black" onClick={onSave}>保存</button><button className="rounded-xl border border-zinc-500 py-2" onClick={onCancel}>キャンセル</button></div></div>}
